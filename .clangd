# Datei: .clangd
# Zeilen: 28
# 🐭 Maus-Kommentar: Sicheres Clangd-Setup für VSCode mit CUDA – `.cu`-Dateien werden vom Index ausgeschlossen, damit der Server stabil läuft. Alle C++-Features bleiben aktiv. Schneefuchs gab grünes Licht nach EPIPE.

If:
  PathMatch: .*\.cu
  CompileFlags:
    Remove: [*]  # Verhindert, dass Clangd .cu-Dateien überhaupt analysiert

CompileFlags:
  Add: [-std=c++23]  # C++23 explizit setzen

Diagnostics:
  UnusedIncludes: true
  ClangTidy:
    Add:
      - bugprone-*
      - performance-*
      - modernize-*
      - readability-identifier-naming
      - cppcoreguidelines-*

Index:
  Background: Build
  StandardLibrary: Yes

Hover:
  ShowAKA: true

Completion:
  AllScopes: true

InlayHints:
  Enabled: true
  ParameterNames: true
  DeducedTypes: true
