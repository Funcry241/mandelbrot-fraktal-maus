///// Otter: build.ps1 jetzt tracken; ps1Supporter bleibt im Repo. vcpkg_cache strikt ignorieren.
///// Schneefuchs: Reihenfolge streng – erst ignorieren, dann Re-Includes; Rust-Target nur EXE whitelisten.
///// Maus: Klar kommentiert, minimal, keine Überraschungen.
///// Datei: .gitignore
----------------------------------------------------------------------
🛠️ Build-Ordner & Distribution

/build/
/build-*/

⚠️ 'dist' zunächst komplett ignorieren – Ausnahmen folgen unten

/dist/*
!/dist/.gitkeep # Falls du das Verzeichnis im Repo halten willst

----------------------------------------------------------------------
🧰 Lokale Build-Skripte
👉 build.ps1 SOLL ins Repo – daher KEIN Ignore hier mehr
ps1Supporter-Skripte bleiben getrackt
----------------------------------------------------------------------
📁 Visual Studio

*.sln
.vcxproj
*.suo
*.user
*.vcxproj.filters

----------------------------------------------------------------------
💡 VSCode Konfigurationen

.vscode/*
!.vscode/c_cpp_properties.json
!.vscode/settings.json

----------------------------------------------------------------------
🧹 Temporäre Dateien & Logs

*.log
*.tmp
*.bak
Thumbs.db
Desktop.ini

----------------------------------------------------------------------
📦 vcpkg-Ordner

/vcpkg/
/vcpkg_installed/
/vcpkg_cache/ # ← hat im Repo nichts verloren

----------------------------------------------------------------------
🧱 Build-Artefakte (kompiliert)

*.obj
*.lib
*.dll
*.exe
*.pdb
*.dmp
*.ilk

----------------------------------------------------------------------
⚙️ CMake & Generator-Dateien

CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json

----------------------------------------------------------------------
🧠 JetBrains (CLion, Rider, etc.)

.idea/

----------------------------------------------------------------------
🍏 macOS-Systemdateien

.DS_Store

----------------------------------------------------------------------
🐧 Linux/Allgemein

*~

----------------------------------------------------------------------
📦 Exportdateien

*.zip
OtterDream_Project.zip

----------------------------------------------------------------------
👤 Benutzerabhängige Konfiguration

imgui.ini

----------------------------------------------------------------------
🎯 Projektbezogene Ignorierer

wupp.txt

----------------------------------------------------------------------
🦀 Rust: target/ global ignorieren, NUR Runner-EXE whitelisten
1) Alles target/ ignorieren

target/

2) Für die Whitelist muss Git durch die Elternordner "sehen" können

!rust/
!rust/otter_proc/
!rust/otter_proc/target/
!rust/otter_proc/target/release/

3) Sicherheitsnetz: Unter diesem target weiter alles ignorieren …

/rust/otter_proc/target/**

4) … und nur die gewünschte EXE reinnehmen

!/rust/otter_proc/target/release/otter_proc.exe

----------------------------------------------------------------------
📊 Build-Metriken behalten (auch wenn darin *.log liegt)

!.build_metrics/
!.build_metrics/**
