# ///// Otter: ps1Supporter-Skripte tracken; build.ps1 lokal. vcpkg_cache strikt ignorieren.
# ///// Schneefuchs: Reihenfolge streng – erst ignorieren, dann Re-Includes; keine doppelten dist-Regeln.
# ///// Maus: Klar kommentiert, minimal, keine Überraschungen.
# ///// Datei: .gitignore

# ----------------------------------------------------------------------
# 🛠️ Build-Ordner & Distribution
/build/
/build-*/

# ⚠️ 'dist' zunächst komplett ignorieren – Ausnahmen folgen unten
/dist/*
!/dist/.gitkeep            # Falls du das Verzeichnis im Repo halten willst

# ----------------------------------------------------------------------
# 🧰 Lokale Build-Skripte
# ps1Supporter soll getrackt werden ⇒ KEIN Ignore hier!
# Nur das Orchestrator-Skript bleibt lokal:
build.ps1

# ----------------------------------------------------------------------
# 📁 Visual Studio
*.sln
*.vcxproj*
*.suo
*.user
*.vcxproj.filters

# ----------------------------------------------------------------------
# 💡 VSCode Konfigurationen
.vscode/*
!.vscode/c_cpp_properties.json
!.vscode/settings.json

# ----------------------------------------------------------------------
# 🧹 Temporäre Dateien & Logs
*.log
*.tmp
*.bak
Thumbs.db
Desktop.ini

# ----------------------------------------------------------------------
# 📦 vcpkg-Ordner
/vcpkg/
/vcpkg_installed/
/vcpkg_cache/          # ← neu: hat im Repo nichts verloren

# ----------------------------------------------------------------------
# 🧱 Build-Artefakte (kompiliert)
*.obj
*.lib
*.dll
*.exe
*.pdb
*.dmp
*.ilk

# ----------------------------------------------------------------------
# ⚙️ CMake & Generator-Dateien
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json

# ----------------------------------------------------------------------
# 🧠 JetBrains (CLion, Rider, etc.)
.idea/

# ----------------------------------------------------------------------
# 🍏 macOS-Systemdateien
.DS_Store

# ----------------------------------------------------------------------
# 🐧 Linux/Allgemein
*~

# ----------------------------------------------------------------------
# 📦 Exportdateien
*.zip
OtterDream_Project.zip

# ----------------------------------------------------------------------
# 👤 Benutzerabhängige Konfiguration
imgui.ini

# ----------------------------------------------------------------------
# 🎯 Projektbezogene Ignorierer
wupp.txt

# ----------------------------------------------------------------------
# 🦀 Rust: target/ global ignorieren, aber Runner-EXE erlauben
# Zuerst alles 'target/' raus …
target/

# … dann den Runner wieder explizit rein (inkl. Elternpfade!)
!rust/
!rust/otter_proc/
!rust/otter_proc/target/
!rust/otter_proc/target/release/
!rust/otter_proc/target/release/otter_proc.exe

# ----------------------------------------------------------------------
# 📊 Build metrics: keep in repo (auch wenn sie *.log enthalten)
!.build_metrics/
!.build_metrics/**
